# Docker 数据库容器使用的 PostgreSQL 密码。请使用独立且高强度的值。
DB_PASSWORD=replace-with-a-strong-database-password
# JWT 签名密钥。上线后请保持不变，否则现有登录会全部失效。
JWT_SECRET=replace-with-at-least-32-random-bytes
# 初始管理员密码；首次登录后请立即修改。
ADMIN_PASSWORD=replace-with-a-strong-initial-password
# 在跨机同步页面保存 SSH 私钥时必填。
# 使用以下命令生成一次：openssl rand -base64 32
# 主服务器和备服务器必须使用完全相同的值；切勿提交真实值到 Git。
SYNC_KEY_ENCRYPTION_KEY=
# 当前部署实例的公开规范 URL（主、备服务器可以不同）。
SITE_URL=http://localhost:3000
# 容器时区；配置每周同步计划时请以此时区为准。
TZ=Asia/Shanghai
